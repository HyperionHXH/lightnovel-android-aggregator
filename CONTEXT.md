# 项目上下文

## 目标

本项目是面向个人账号的 Android 轻小说聚合阅读器。当前内置接入轻之国度与轻书架，统一搜索、详情、目录、正文、阅读进度和账号入口，同时隔离每个来源的认证、网络协议与错误。

## 核心术语

- **来源（Source）**：一个独立内容服务。目前为轻之国度和轻书架。
- **来源 ID（sourceId）**：来源的稳定本地标识，不是站点返回的数字 ID。
- **远端 ID（remoteId）**：资源在某个来源内部的标识；它只有与 `sourceId` 组合后才唯一。
- **来源资源 ID（SourceResourceId）**：`(sourceId, remoteId)` 组合键，用于图书、章节、缓存、导航和阅读进度。
- **来源适配器（Source Adapter）**：把某站认证、搜索、详情和阅读协议映射为统一能力接口的内置实现。
- **聚合搜索（Aggregate Search）**：并行查询所有可搜索来源，逐来源返回成功或失败，不让单站故障阻断另一站。
- **来源错误（Source Error）**：保留具体来源及认证、限流、超时、解析、权限或内容不存在等类型的失败。
- **在线进度（Local Progress）**：以来源资源 ID 保存于设备上的在线阅读位置，不等同于任一站的云端历史。

## 不变量

- 缓存、导航、书架和阅读进度不得只用 `remoteId` 作为业务键。
- 两站登录态彼此独立，凭据不得跨来源共享。
- 聚合操作允许部分成功，错误提示必须指出具体来源。
- 正文与图片请求的站点兼容逻辑留在对应来源适配器或其网络层，不进入通用 UI。
- 首版只运行随 App 编译的适配器，不下载或执行第三方插件代码。

## 当前边界

已完成双源聚合搜索、双源独立发现、统一书架、来源账号入口、来源感知详情/目录/阅读、在线阅读进度、聚合阅读历史、来源更新快照和应用内离线下载。发现页只在轻之国度与轻书架之间切换各自榜单；书架只提供“全部 / 已下载”，在线书卡统一标注来源。轻之国度会使用来源提供的未读章数，其他来源只有在章节数跨刷新增加且已有基线时才标记更新；更新始终以 `(sourceId, remoteId)` 为键，书架会按来源汇总更新数量；“全部标为已读”只确认本地提示，不修改远端历史。轻书架章节正文依赖服务端下发的专用字体，在线阅读与离线下载都必须先持久缓存并加载该字体，字体失败时不得显示混淆正文。后台更新提醒已实现为默认关闭的 6 小时 WorkManager 检查，启用需通知权限；在线章节 EPUB 3 文本导出和系统文件保存入口已实现，离线书库可在设置中迁移到用户授权的 SAF 文件夹。
